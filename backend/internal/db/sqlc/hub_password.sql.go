// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: hub_password.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const changeHubPassword = `-- name: ChangeHubPassword :one
WITH updated AS (
    UPDATE vetchium.hub_users AS u
    SET password_hash = $1,
        updated_at = now()
    WHERE u.hub_user_did = $2
      AND u.hub_user_state = 'active'
    RETURNING u.hub_user_did
), sessions AS (
    DELETE FROM vetchium.hub_sessions
    WHERE hub_user_did IN (SELECT hub_user_did FROM updated)
      AND hub_session_id <> $3
), challenges AS (
    UPDATE vetchium.hub_login_challenges
    SET active = false
    WHERE hub_user_did IN (SELECT hub_user_did FROM updated)
      AND active
), resets AS (
    UPDATE vetchium.hub_password_reset_tokens
    SET active = false
    WHERE hub_user_did IN (SELECT hub_user_did FROM updated)
      AND active
), audit AS (
    INSERT INTO vetchium.audit_events (
        tenant_id, action, entity_type, entity_id, actor_type, actor_id,
        source, payload
    )
    SELECT
        $4,
        'hub.password.changed',
        'hub_user',
        hub_user_did::text,
        'hub_user',
        hub_user_did::text,
        'hub-api',
        jsonb_build_object(
            'password_changed', true,
            'other_sessions_revoked', true
        )
    FROM updated
    RETURNING audit_event_id
)
SELECT EXISTS (SELECT 1 FROM audit) AS changed
`

type ChangeHubPasswordParams struct {
	PasswordHash        string      `json:"password_hash"`
	HubUserDid          pgtype.UUID `json:"hub_user_did"`
	CurrentHubSessionID pgtype.UUID `json:"current_hub_session_id"`
	TenantID            string      `json:"tenant_id"`
}

func (q *Queries) ChangeHubPassword(ctx context.Context, arg ChangeHubPasswordParams) (bool, error) {
	row := q.db.QueryRow(ctx, changeHubPassword,
		arg.PasswordHash,
		arg.HubUserDid,
		arg.CurrentHubSessionID,
		arg.TenantID,
	)
	var changed bool
	err := row.Scan(&changed)
	return changed, err
}

const completeHubPasswordReset = `-- name: CompleteHubPasswordReset :one
WITH token AS (
    SELECT t.hub_password_reset_token_id, t.hub_user_did
    FROM vetchium.hub_password_reset_tokens AS t
    JOIN vetchium.hub_users AS u USING (hub_user_did)
    WHERE t.token_hash = $1
      AND t.active
      AND t.consumed_at IS NULL
      AND t.expires_at > now()
      AND u.hub_user_state = 'active'
    FOR UPDATE OF t
), updated AS (
    UPDATE vetchium.hub_users AS u
    SET password_hash = $2,
        updated_at = now()
    WHERE hub_user_did = (SELECT hub_user_did FROM token)
    RETURNING hub_user_did
), consumed AS (
    UPDATE vetchium.hub_password_reset_tokens
    SET consumed_at = now(),
        active = false
    WHERE hub_password_reset_token_id = (
        SELECT hub_password_reset_token_id FROM token
    )
      AND EXISTS (SELECT 1 FROM updated)
    RETURNING hub_password_reset_token_id, hub_user_did
), invalidated_resets AS (
    UPDATE vetchium.hub_password_reset_tokens
    SET active = false
    WHERE hub_user_did IN (SELECT hub_user_did FROM updated)
      AND active
), sessions AS (
    DELETE FROM vetchium.hub_sessions
    WHERE hub_user_did IN (SELECT hub_user_did FROM updated)
), challenges AS (
    UPDATE vetchium.hub_login_challenges
    SET active = false
    WHERE hub_user_did IN (SELECT hub_user_did FROM updated)
      AND active
), audit AS (
    INSERT INTO vetchium.audit_events (
        tenant_id, action, entity_type, entity_id, actor_type, actor_id,
        source, idempotency_key, payload
    )
    SELECT
        $3,
        'hub.password.reset',
        'hub_user',
        hub_user_did::text,
        'anonymous',
        NULL,
        'hub-api',
        $4,
        jsonb_build_object(
            'password_changed', true,
            'all_sessions_revoked', true
        )
    FROM consumed
    RETURNING audit_event_id
)
SELECT EXISTS (SELECT 1 FROM audit) AS completed
`

type CompleteHubPasswordResetParams struct {
	ResetTokenHash []byte      `json:"reset_token_hash"`
	PasswordHash   string      `json:"password_hash"`
	TenantID       string      `json:"tenant_id"`
	IdempotencyKey pgtype.Text `json:"idempotency_key"`
}

func (q *Queries) CompleteHubPasswordReset(ctx context.Context, arg CompleteHubPasswordResetParams) (bool, error) {
	row := q.db.QueryRow(ctx, completeHubPasswordReset,
		arg.ResetTokenHash,
		arg.PasswordHash,
		arg.TenantID,
		arg.IdempotencyKey,
	)
	var completed bool
	err := row.Scan(&completed)
	return completed, err
}

const createHubPasswordReset = `-- name: CreateHubPasswordReset :one
WITH eligible_user AS (
    SELECT hub_user_did, email_address, preferred_language
    FROM vetchium.hub_users
    WHERE email_address = $1
      AND hub_user_state = 'active'
), reset AS (
    INSERT INTO vetchium.hub_password_reset_tokens (
        hub_user_did,
        token_hash,
        expires_at
    )
    SELECT
        hub_user_did,
        $2,
        $3
    FROM eligible_user
    ON CONFLICT (hub_user_did) WHERE active DO UPDATE
    SET token_hash = EXCLUDED.token_hash,
        created_at = now(),
        expires_at = EXCLUDED.expires_at,
        consumed_at = NULL
    RETURNING hub_password_reset_token_id, hub_user_did
), outbox AS (
    INSERT INTO vetchium.hub_email_outbox (
        kind,
        recipient_email_address,
        preferred_language,
        payload_ciphertext
    )
    SELECT
        'password-reset',
        u.email_address,
        u.preferred_language,
        $4
    FROM eligible_user AS u
    WHERE EXISTS (SELECT 1 FROM reset)
    RETURNING hub_email_outbox_id
), audit AS (
    INSERT INTO vetchium.audit_events (
        tenant_id, action, entity_type, entity_id, actor_type, actor_id,
        source, idempotency_key, payload
    )
    SELECT
        $5,
        'hub.password-reset.requested',
        'hub_password_reset',
        hub_password_reset_token_id::text,
        'anonymous',
        NULL,
        'hub-api',
        $6,
        jsonb_build_object('email_queued', EXISTS (SELECT 1 FROM outbox))
    FROM reset
)
SELECT EXISTS (SELECT 1 FROM outbox) AS queued
`

type CreateHubPasswordResetParams struct {
	EmailAddress      string             `json:"email_address"`
	TokenHash         []byte             `json:"token_hash"`
	ExpiresAt         pgtype.Timestamptz `json:"expires_at"`
	PayloadCiphertext []byte             `json:"payload_ciphertext"`
	TenantID          string             `json:"tenant_id"`
	IdempotencyKey    pgtype.Text        `json:"idempotency_key"`
}

func (q *Queries) CreateHubPasswordReset(ctx context.Context, arg CreateHubPasswordResetParams) (bool, error) {
	row := q.db.QueryRow(ctx, createHubPasswordReset,
		arg.EmailAddress,
		arg.TokenHash,
		arg.ExpiresAt,
		arg.PayloadCiphertext,
		arg.TenantID,
		arg.IdempotencyKey,
	)
	var queued bool
	err := row.Scan(&queued)
	return queued, err
}

const resolveHubPasswordResetUser = `-- name: ResolveHubPasswordResetUser :one
SELECT hub_user_did
FROM vetchium.hub_password_reset_tokens
WHERE token_hash = $1
  AND active
  AND consumed_at IS NULL
  AND expires_at > now()
`

func (q *Queries) ResolveHubPasswordResetUser(ctx context.Context, tokenHash []byte) (pgtype.UUID, error) {
	row := q.db.QueryRow(ctx, resolveHubPasswordResetUser, tokenHash)
	var hub_user_did pgtype.UUID
	err := row.Scan(&hub_user_did)
	return hub_user_did, err
}
